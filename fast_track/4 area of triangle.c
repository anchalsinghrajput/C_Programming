#include <stdio.h>
int main()

{   
    float base , height, area;
      printf("\nEnter base and height of the triangle:");
      scanf("%f  %f",&base,&height);
      area=(base*height)/2 
      printf("\n  area of the triangle with base %.1f and height %.1f is %.1f",                 base,height,area);

    return 0;
}

